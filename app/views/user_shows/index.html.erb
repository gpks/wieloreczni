<table class="table table-hover">
   <thead>
    <tr>
    <th>Użytkownik</th>
    <th>Liczba gier</th>
    

    </tr>
  </thead>

   <% @users.each do |user| %>
      <tr>
         <td><%= link_to "#{user.username}", user_show_path(user)  %></td>
          <td><%= "Liczba gier" %></td>
        
      </tr>
    <% end %>
</table>