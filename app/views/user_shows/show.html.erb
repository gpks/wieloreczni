<div>
<h2>Nazwa użytkownika: <%= @user.username %></h2>
</div>

<table class="table table-hover">
<thead>
    <tr>
    
    <th>Nazwa gry</th>
    <th>Rodzaj gry</th>
    <th>Ocena</th>
    <th>Usuń z półki</th>
       </tr>
  </thead>

   <% @user.games.each do |game| %>
      <tr>
      
       
        <td><%= game.name %></td>
        <td><%= game.genre %></td>
        <% if Rating.where(game_id: game.id, user_id: @user.id).last %>
       <td> <%= Rating.where(game_id: game.id, user_id: @user.id).last.points %> 
      
        </td>
        <%else%>
        <td>
        <%= "Jeszcze nie oceniona"%>
        <% end %>
        <td><%= button_to 'remove', removegame_user_show_path(:type=> game), method: :post, class: "btn btn-default" %></td> 
      </tr>
      <% end %> 
      
    
</table>