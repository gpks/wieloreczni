<div class="page-header">
<h1><%= @game.name %></h1>
</div>
<div class="panel panel-default">
  <div class="panel-body">
   <%= @game.description %>
  </div>
</div>

<table class="table table-hover">
<thead>
    <tr>
    
    <th>Typ gry</th>
    <th>Minimalna liczba graczy</th>
    <th>Maksymalna liczba graczy</th>
    <th>Minimalny wiek graczy</th>
    <th>Średnia ocena</th>
    <% if user_signed_in? %>
        <th><%= "Oceń grę" %></th>
        <th><%= "Dodaj recenzję" %></th>
        <% end %>

    </tr>
  </thead>

   
      <tr>
       
        <td><%= @game.genre %></td>
        <td><%= @game.minplayer %></td>
        <td><%= @game.maxplayer %></td>
        <td><%= @game.minage %></td>
        <td><%= @game.ratings.average(:points) %></td> 
        <% if user_signed_in? %>
        <td><%= "tu będzie rating" %></td>
        <td><%= "tu będzie dodawanie recenzji" %></td>
        <% end %>
      </tr>
    
</table>


   
      
      <% @game.ratings.each do |rating| %>
      
      <%if rating.review%>
      <div class="panel panel-default">
  <div class="panel-body">
    <%= rating.review %>
  </div>
  <div class="panel-footer">Użytkownik: <%= User.find(rating.user_id).email %></div>
</div> 
      
        <% end %>
        <% end %>

<div>Dodaj własną recenzję </div>
<%= render "ratings/form"%>